<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_main"
    app:startDestination="@id/navHome">

    <navigation android:id="@+id/navHome"
        app:startDestination="@id/homeDriverFragment">
        <fragment
            android:id="@+id/homeDriverFragment"
            android:name="com.example.truckercore.business_driver.view.fragments.nav_home.home.HomeDriverFragment"
            android:label="fragment_home_driver"
            tools:layout="@layout/fragment_home_driver" />
    </navigation>
    <navigation android:id="@+id/navDocument"
        app:startDestination="@id/documentListFragment">
        <fragment
            android:id="@+id/documentListFragment"
            android:name="com.example.truckercore.business_driver.view.fragments.nav_documents.document_list.DocumentListFragment"
            android:label="fragment_document_list"
            tools:layout="@layout/fragment_document_list" />
    </navigation>
</navigation>