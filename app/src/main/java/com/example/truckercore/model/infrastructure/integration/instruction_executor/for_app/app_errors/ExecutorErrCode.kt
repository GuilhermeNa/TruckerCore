package com.example.truckercore.model.infrastructure.integration.instruction_executor.for_app.app_errors

import com.example.truckercore.model.errors.ErrorCode

/**
 * Interface representing error codes that are specific to the execution of instructions within the system.
 *
 * The [ExecutorErrCode] interface is used to define a set of error codes related to the execution of instructions.
 * These error codes help categorize and provide specific information about failures that occur during instruction execution.
 */
interface ExecutorErrCode: ErrorCode